
version: "3.9"
services:
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
  speed-detection:
    build: .
    env_file: .env
    environment:
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
    depends_on:
      - redis
    ports:
      - "8000:8000"
